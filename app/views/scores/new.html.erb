<h1>New score</h1>

<%= render "form", score: @score %>

<br>

<div>
  <% if (session[:who] == 'scoreAll') %>
    <%= link_to "Back to scores", scores_path %>
  <% else %>
    <%= link_to "Back to scores", "/students/#{@score.student_id}/edit_score" %>
  <% end%>
</div>
